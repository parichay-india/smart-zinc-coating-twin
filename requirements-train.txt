# Extra dependencies needed only to regenerate the model bundle (python train_models.py
# or the Colab notebook). Not required to run the deployed Streamlit app.
-r requirements.txt
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
scipy>=1.11
matplotlib>=3.7
